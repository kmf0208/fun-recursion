const sum = require("../Recursion.js");

// 
describe("sum", () => {
    test("Implement a function that sums the integers 1 through n.", () => {
        expect(sum(1)).toBe(1);
        expect(sum(2)).toBe(3);
        expect(sum(5)).toBe(15);
    })
})












// desciribe("", () => {
//     test("", () => {
        
//     })
// })